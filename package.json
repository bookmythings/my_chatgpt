{
  "name": "replit-clone",
  "version": "1.0.0",
  "description": "A cloud-based IDE clone of Replit with code editing, execution, and collaboration features",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "test": "cd frontend && npm test && cd ../backend && npm test",
    "lint": "cd frontend && npm run lint && cd ../backend && npm run lint"
  },
  "keywords": ["ide", "coding", "replit", "clone", "online-editor", "code-execution"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}